# Устанавливает переменную среды
ARG ADMINER_VERSION

# Устанавливает базовый образ Adminer с использованием переменной среды
FROM adminer:${ADMINER_VERSION}

# Описывает открытый порт внутри контейнера
EXPOSE 8080
