# Устанавливает переменную среды
ARG MYSQL_VERSION

# Устанавливает базовый образ MySQL с использованием переменной среды
FROM mysql:${MYSQL_VERSION}

# Описывает открытый порт внутри контейнера
EXPOSE 3306
